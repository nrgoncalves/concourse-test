---
platform: linux

image_resource:
  type: docker-image
  source: { repository: python }

inputs:
  - name: concourse-test-repo

run:
  path: /bin/sh
  args:
    - -c
    - |
      pip install pytest
      ls -al
      pytest test_mymodule.py
